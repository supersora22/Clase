package repaso_examen;

public interface Tratable {
    // hacemos esta interfaz con un metodo que devuelve un boolean para que luego cada pokemon decida si necesita pasar a la UCI o no
    public boolean necesitaUrgencia();
}